name: study_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.5.3
  file_picker: ^10.3.3
  open_filex: ^4.7.0

  provider: ^6.1.5 # ✅ 들여쓰기 한 단계 줄임 (flutter 블록 밖으로 이동)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
